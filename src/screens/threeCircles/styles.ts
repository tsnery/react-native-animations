import { StyleSheet } from "react-native";

export const firstStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#FFF",
  },
  circle: {
    position: "absolute",
    height: 80,
    aspectRatio: 1,
    backgroundColor: "blue",
    borderRadius: 40,
    opacity: 0.8,
  },
});
